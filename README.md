# E-mail-Spam-Classiefier
This algorithm classify e-mail from a Kaggle database. The dataset consist of e-mail in two categories SPAM and HAM (which aren't spam) that'll be going through a support vector machine (SVM) to be trained and tested for a new dataset inserted by the user at the end of the algorithm. Based on the dataset the best accuracy we can get is 0.94
